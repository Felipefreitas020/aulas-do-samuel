<?php
if(isset($_POST['logupBtn'])) {
    $userEmail = $_post['email'];
    $userPassword = $_POST['Password']
        if (userEmail === '' || $userPassword ===) {
            echo "O campo de email ou senha está vazio. Por favor, tente novamente";
        }else {
            $sql = "INSERT INTO usuarios (email_user, name_user, Password_user, phone_user,
            cpf_user, birth_user)" VALUES
        }
        }
}